// Shared commitlint preset for btravstack repos: conventional commits.
// Consume from a repo's commitlint.config.js:
//   export default { extends: ["@btravstack/commitlint"] };
export default {
  extends: ["@commitlint/config-conventional"],
};
